export type MessageStatus = 'accepted' | 'sent' | 'acked' | 'stored' | 'consumed' | 'failed' | 'duplicate';
/** Valid state transitions */
export declare const STATUS_TRANSITIONS: Record<MessageStatus, MessageStatus[]>;
export declare function isValidTransition(from: MessageStatus, to: MessageStatus): boolean;
export interface DeliveryReceipt {
    messageId: string;
    status: MessageStatus;
    nodeId: string;
    ts: number;
    /** Optional: trace of all state transitions */
    trace?: StatusTransition[];
}
export interface StatusTransition {
    from: MessageStatus;
    to: MessageStatus;
    nodeId: string;
    ts: number;
}
export interface ReadReceipt {
    messageId: string;
    readBy: string;
    ts: number;
}
export interface TypingIndicator {
    fromAgent: string;
    threadId: string;
    isTyping: boolean;
}
//# sourceMappingURL=receipt.d.ts.map