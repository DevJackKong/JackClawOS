export type WatchdogEventType = 'task_deviation' | 'memory_anomaly' | 'scope_violation' | 'trust_drop' | 'collab_overstep' | 'override_attempt';
export type AlertSeverity = 'info' | 'warning' | 'critical';
export interface WatchdogEvent {
    id: string;
    type: WatchdogEventType;
    severity: AlertSeverity;
    nodeId: string;
    watcherNodeId: string;
    description: string;
    evidence: Record<string, unknown>;
    timestamp: number;
    acknowledged: boolean;
    acknowledgedBy?: string;
}
export interface WatchdogPolicy {
    watcherHandle: string;
    targetHandle: string;
    scope: 'self' | 'granted';
    permissions: ('read_tasks' | 'read_memory' | 'read_collabs' | 'read_trust')[];
    alertChannels: ('feishu' | 'telegram' | 'webhook')[];
    webhookUrl?: string;
    createdAt: number;
    grantedAt?: number;
    revokedAt?: number;
}
export interface WatchdogSnapshot {
    nodeId: string;
    snapshotId: string;
    tasks: unknown[];
    memoryHash: string;
    trustScores: Record<string, number>;
    timestamp: number;
}
export interface GetAlertsOptions {
    severity?: AlertSeverity;
    acknowledged?: boolean;
    limit?: number;
    since?: number;
}
//# sourceMappingURL=types.d.ts.map