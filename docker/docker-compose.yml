version: '3.8'

services:
  jackclaw:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3100:3100"
      - "19000:19000"
    env_file:
      - ../.env
    restart: unless-stopped
    volumes:
      - jackclaw-data:/app/data

volumes:
  jackclaw-data:
