/**
 * IsolationStore — Watchdog 隔离存储层
 *
 * 规则：
 *  - 日志写入 ~/.jackclaw/watchdog/<nodeId>/
 *  - 文件写入后立即 chmod 444（只读），防止任何进程覆盖
 *  - 快照以 gzip 压缩存储
 *  - 本模块不暴露任何删除/覆盖接口
 */
import { WatchdogEvent, WatchdogSnapshot } from './types';
/**
 * 追加事件行到 events.jsonl（每行一个 JSON）
 * 每次追加都重建只读保护
 */
export declare function appendEvent(nodeId: string, event: WatchdogEvent): void;
/**
 * 读取所有事件（只读访问）
 */
export declare function readEvents(nodeId: string): WatchdogEvent[];
/**
 * 更新单个事件（用于 humanAck）
 * 重写整个文件（先解锁，写入，再锁定）
 */
export declare function updateEvent(nodeId: string, updatedEvent: WatchdogEvent): boolean;
/**
 * 写入策略变更审计日志（只追加）
 */
export declare function appendPolicyAudit(entry: Record<string, unknown>): void;
/**
 * 保存快照（gzip 压缩）
 */
export declare function saveSnapshot(snapshot: WatchdogSnapshot): void;
/**
 * 读取快照
 */
export declare function loadSnapshot(nodeId: string, snapshotId: string): WatchdogSnapshot | null;
/**
 * 列出某节点的所有快照 ID（按时间倒序）
 */
export declare function listSnapshotIds(nodeId: string): string[];
//# sourceMappingURL=isolation.d.ts.map