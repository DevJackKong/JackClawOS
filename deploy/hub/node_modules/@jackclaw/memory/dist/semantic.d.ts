import type { MemoryEntry } from './types.js';
/** 综合相似度（字符串 + 关键词各占 50%） */
export declare function similarity(a: string, b: string): number;
/** 判断两个 entry 是否足够相似，可以合并 */
export declare function isSimilar(a: MemoryEntry, b: MemoryEntry, threshold?: number): boolean;
/**
 * 压缩 entry 列表，合并相似条目
 * - 保留 importance 较高的一条
 * - 合并 tags
 * - 返回合并数量
 */
export declare function compress(entries: MemoryEntry[]): {
    entries: MemoryEntry[];
    merged: number;
};
//# sourceMappingURL=semantic.d.ts.map