import type { MemoryEntry, NodeRef, MemDir } from './types.js';
export declare class MemDirSync {
    private nodeId;
    constructor(nodeId: string);
    /**
     * 把本地 project/reference 类型的记忆条目推送到 Hub。
     * 推送前用 HMAC-SHA256 签名请求体，Hub 侧校验完整性。
     */
    push(entries: MemDir[], targetHubUrl: string): Promise<void>;
    /**
     * 从 Hub 拉取其他节点共享的 project/reference 记忆。
     * 请求同样附带 HMAC 签名供 Hub 认证。
     */
    pull(nodeId: string, hubUrl: string): Promise<MemDir[]>;
}
export declare class HubSync {
    private agentId;
    private hubUrl;
    constructor(agentId: string, hubUrl: string);
    /** 推送记忆到 org L3 */
    pushToOrg(entry: MemoryEntry): Promise<void>;
    /** 从 Hub 拉取 org L3 记忆 */
    pullFromOrg(): Promise<MemoryEntry[]>;
    /** 注册自己的技能到 Hub */
    registerSkills(name: string, skills: string[]): Promise<void>;
    /** 查找拥有某技能的 Agent */
    findExpert(skill: string): Promise<NodeRef[]>;
    /** 发起协作会话 */
    initCollab(peerId: string, intent: string, topic?: string): Promise<string>;
    /** 同步协作记忆到 Hub */
    syncCollab(sessionId: string, entries: MemoryEntry[]): Promise<void>;
    /** 结束协作，返回对方的教学条目 */
    endCollab(sessionId: string, mode: string): Promise<MemoryEntry[]>;
}
//# sourceMappingURL=sync.d.ts.map